source ~/.bashrc

# .bashrc should be ln -s'd to ~ directory, from repo, as should this,
# as .bash_profile is what terminal loads when opened, and .bashrc is
# used for other instances, and all code should be kept in one spot.
